using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WW11_badBrain : MonoBehaviour
{
    public int choose;
    // Start is called before the first frame update
    void Start()
    {
        choose = Random.Range(1, 4);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
